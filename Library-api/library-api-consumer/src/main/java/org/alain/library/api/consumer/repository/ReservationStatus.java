package org.alain.library.api.consumer.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservationStatus extends JpaRepository<ReservationStatus, Long> {
}
